#include <bits/stdc++.h>
using namespace std;

void solve

    int
    main() {
    int t;
    cin >> t;
    vector<long long> w, p;
    while (t--) {
        int n;
        w.resize(n);
        p.resize(n);
        for (int i = 0; i < n; i++) {
            cin >> w[i] >> p[i];
        }

    }
    return 0;
}
